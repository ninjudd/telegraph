define({
  from:   "Time to use as the start of the graph time span. Can be one of: a negative offset relative to now, a positive offset relative to until, an absolute date as YYYY-MM-DD (or MM/DD), or seconds since the epoch.",
  until:  "Time to use as the end of the graph time span. Can be one of: a negative offset relative to now, a positive offset relative to from, an absolute date as YYYY-MM-DD (or MM/DD), or seconds since the epoch.",
  period: "Interval to use for all periodic query operators.",
});

